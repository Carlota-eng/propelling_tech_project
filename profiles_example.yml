my_project_name:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: snowflake_account
      user: your_user
      password: your_password
      role: transform
      warehouse: compute_wh
      database: dev_db
      schema: analytics